# AI Financial Health Monitor

A minimal Flask application that estimates a user's financial health score based on basic financial inputs.

## Quick Start (Windows PowerShell)

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```

Then open:

http://127.0.0.1:5000